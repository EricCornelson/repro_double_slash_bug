{
    "configurations": [
        {
            "name": "Launch Chrome",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost:5000//somepage.html",
            "webRoot": "${workspaceFolder}/wwwroot"
        }
    ]
}